{
  "name": "medario-functions",
  "description": "Cloud Functions for Medário — interest aggregation and affinity computation",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "test:rules": "node --test firestore.rules.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0"
  },
  "engines": {
    "node": "22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
